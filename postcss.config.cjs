// CommonJS PostCSS config — keep as CJS because file uses .cjs
// PostCSS config: keep minimal to avoid loading Tailwind as a PostCSS plugin
module.exports = {
  plugins: {
    autoprefixer: {},
  },
}
